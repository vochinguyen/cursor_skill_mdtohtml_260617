# MD to HTML Converter Skill

This repository contains a skill designed to convert Markdown (`.md`) files into clean HTML output.

## Features
*   Converts standard Markdown syntax to HTML.
*   Includes necessary assets like mermaid diagrams for enhanced rendering.

## Usage
(Instructions on how to use this skill will go here)

## Prerequisites
*   Node.js and npm (if running locally).